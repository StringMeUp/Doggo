<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="androidx.recyclerview.widget.GridLayoutManager" />

        <variable
            name="moreDogsViewModelModel"
            type="com.example.doggo.viewmodel.MoreDogsViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_dog"
        tools:context=".view.MoreDogsFragment">

        <TextView
            android:id="@+id/text_title"
            android:layout_width="300dp"
            android:layout_height="120dp"
            android:layout_marginTop="70dp"
            android:background="@drawable/bone_background"
            android:gravity="center"
            android:text="@string/moreDogs_TextView"
            android:textColor="@color/colorBlack"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toTopOf="@id/recyclerView_breeds"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView_breeds"
            android:layout_width="match_parent"
            android:layout_height="360dp"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="150dp"
            android:padding="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="@id/text_title"
            app:layout_constraintRight_toRightOf="@id/text_title"
            app:layout_constraintTop_toBottomOf="@id/text_title"
            tools:listitem="@layout/more_dogs_image" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>